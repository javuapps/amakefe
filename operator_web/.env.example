# The operator console. Deployed on its own domain, separate from the studio.
#
# Everything here is compiled into the browser bundle and is public. The
# service-role key and the Lenco API key are Edge Function secrets, set in
# Supabase, never here — this app never touches a payment provider directly.

VITE_SUPABASE_URL=https://rubukvxtiezyfxzqacsi.supabase.co
VITE_SUPABASE_KEY=sb_publishable_duXTls5l0OX4gtP2FFZSGg_0vbdMFWt
