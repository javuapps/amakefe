{
  "name": "amakefe",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:reader": "pnpm --filter reader_web dev",
    "dev:author": "pnpm --filter author_web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
