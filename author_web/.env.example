# The studio. Deployed at https://gracious.javuapps.com
#
# Everything here is compiled into the browser bundle and is public. No secret
# belongs in this file — the service-role key and the Meta app secret are Edge
# Function secrets, set in Supabase, never here.

VITE_SUPABASE_URL=https://rubukvxtiezyfxzqacsi.supabase.co
VITE_SUPABASE_KEY=sb_publishable_duXTls5l0OX4gtP2FFZSGg_0vbdMFWt

# Where a published story lives for a reader. This is the link appended to every
# Facebook post, so it must be the READER's domain, not the studio's.
VITE_PUBLIC_SITE_URL=https://amakefe.javuapps.com

# The Meta app that publishes to the Facebook Page — "author" because there are
# two, and they must never be confused. This one is Facebook Login for Business
# and is used only here, to build the consent URL when connecting a Page.
#
# The other app authenticates READERS. It is a plain Facebook Login app, its
# credentials live only in Supabase (Authentication -> Providers -> Facebook),
# and it appears in no environment file anywhere.
VITE_META_AUTHOR_APP_ID=2255628145003686
